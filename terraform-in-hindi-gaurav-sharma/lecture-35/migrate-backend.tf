terraform {
  # Create terraform with below backend, run terraform apply and then comment this code and run terraform apply again
  # backend "s3" {
  #   bucket = "terraform-state-bucket-raman"
  #   key    = "terraform.tfstate" # name of the tfstate file in the bucket
  #   region = "ap-south-1"
  # }
}